/// USPACE Design System Corner Radius Tokens
///
/// 來源：Figma Variables — Mode 1.tokens.json (Number group)
///
/// Figma scope: CORNER_RADIUS
class USpaceRadius {
  USpaceRadius._();

  // ─── Corner Radius ───────────────────────────────────────
  /// Small: 8px（小圓角，如 card 內元素）
  static const double small = 8;

  /// Medium: 20px（中圓角，如 dropdown panel、popup）
  static const double medium = 20;

  /// Full: 1000px（完全圓角，如 button、chip、input）
  static const double full = 1000;
}
